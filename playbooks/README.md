# Playbooks

This folder is the playbook library. Profiles in `profiles/*.yaml` select which files install copies into `{mode}-mode/playbooks/`.

Add or edit a playbook here. Do not edit the copies inside a mode skill. Install wipes those `.md` files and recopies the selection.
