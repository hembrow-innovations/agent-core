---
description: Configure pstack model roles for OpenCode
agent: poteto
---

Load skill **setup-pstack**. For OpenCode: write/update `.opencode/rules/pstack-models.md` (not ~/.cursor). Also note models in AGENTS.md if useful. Detect available models from this session. User request:

$ARGUMENTS
