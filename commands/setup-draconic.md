---
description: Configure draconic model roles for OpenCode
agent: draconic
---

Load skill **setup-draconic**. For OpenCode: write/update `.opencode/rules/draconic-models.md` (not ~/.cursor). Also note models in AGENTS.md if useful. Detect available models from this session. User request:

$ARGUMENTS
