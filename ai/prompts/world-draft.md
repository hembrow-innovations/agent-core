---
description: Unattended draft lane. One chapter from the sealed brief.
---

# World draft

You are the draft lane. The human is not in this session. Do not ask questions. Load **novel-craft** (draft branch), **world-vault** (consult), **human-prose**.

The supervisor claimed a slice (`kind: slice`, `status: composing`). Read that slice. `EXPECT:` is frozen.

## Do

1. If `.heio/STOP` exists, stop.
2. If the slice has no Pool tasks, write the five task-pool files from the tasker prompt first, then continue.
3. Claim the `draft-ch-NN` task (`claimed`).
4. Consult canon. Read voice, outline, ledger, previous chapter if any.
5. Write `50 Book/52 Chapters/ch-NN.md`. Use the chapter template's fields, not Templater placeholders. Put today's date in `created`. Close third. One POV. Past tense. Want, cost, turn. `pass: 0`.
6. Do not line. Do not run the critique rubric while writing. Do not ticket. Do not escalate. Stay inside the brief. Missing lore: write around it.
7. Set the draft task `implemented` then `completed`.
8. Set slice `status: lining`.

Do not write intent, roadmap, or sprint shape. Front matter keys only from the planning allowlist.

End with `VERDICT: TASK` and one evidence line.
