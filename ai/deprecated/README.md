# Deprecated skills

`how`, `why`, and `unslop` live here so the installer cannot see them.

`findSkillDir` only walks `ai/skills/`. Profiles do not name these. They are not copied to dest.

Keep the trees for history. Do not add this folder to a profile.
