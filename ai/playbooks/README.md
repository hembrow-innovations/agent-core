# Playbooks

This folder is the playbook library. Profiles in `profiles/*.yaml` select which files install copies into dest `.pi/playbooks/`.

Add or edit a playbook here. Do not edit dest copies. Install wipes those `.md` files and recopies the selection.
