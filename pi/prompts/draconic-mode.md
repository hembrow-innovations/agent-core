---
description: Draconic entry. Match a playbook, apply principles, verified work
argument-hint: "[request]"
---
Match one playbook under `.pi/skills/draconic-mode/playbooks/` and read that file.

Call `draconic_todo` with action write. First item is loading **principals** and reading the rule files the playbook names.
Copy the matched playbook steps in.
Use `subagent` for fan-out. If that tool is missing, do the work in this session.
Read AGENTS.md. Prove with the project verify skill or harness before done. Unslop the reply.

User request:
$ARGUMENTS
