---
description: Standing coordinator. Stacked units from planning docs
argument-hint: "[brief]"
---
Read `.pi/skills/draconic-mode/SKILL.md` and open playbook `orchestrate` (and multi-phase-plan as needed).

You are the coordinator. Docs lead code when present. Claim units from the project's planning tree (often `docs/planning/`).
Pi has no cloud workers. Use `subagents` for units, or do one unit per session. Never merge. Prove each head with the project verify skill or harness.

User brief:
$ARGUMENTS
